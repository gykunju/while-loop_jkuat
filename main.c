#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a=2,b=7;
    while(a < b)
    {
        printf("adding 1 to %d\n",a);
        a++;
    }
    printf("a and b are equal now");
    return 0;
}
